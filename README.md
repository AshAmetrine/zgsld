# ZGSLD

zgsld (zealous greeter+session launcher daemon), is a daemon for launching greeters, handling PAM authentication through IPC, and launching a session.

## Usage

## Greeter Development

A greeter can be built either as a separate executable or bundled with zgsld to produce a single executable which will call itself to launch the greeter.

